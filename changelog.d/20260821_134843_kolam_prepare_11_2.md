<!--
A new scriv changelog fragment.

Uncomment the section that is right (remove the HTML comment wrapper).
For top level release notes, leave all the headers commented out.
-->

<!--
### Breaking

- A bullet item for the Breaking category.

-->
### Non-Breaking

- Update Shelley ledger integration for new ledger API: replace deprecated
  `Validated` type with `ValidatedTx`, update `GetPoolDistr2` result type to
  `QueryResultPoolDistr`, remove `EncCBORGroup` constraint from
  `ShelleyCompatible`, add `EncCBOR`/`DecCBOR` `Block` constraints, and fix
  `UpgradeDijkstraPParams` and `StakePoolParams` fields.
<!--
### Patch

- A bullet item for the Patch category.

-->
