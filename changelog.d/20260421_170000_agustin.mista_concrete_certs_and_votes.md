<!--
A new scriv changelog fragment.

Uncomment the section that is right (remove the HTML comment wrapper).
For top level release notes, leave all the headers commented out.
-->

<!--
### Breaking

- A bullet item for the Breaking category.

-->

### Non-Breaking

- Introduce `Ouroboros.Consensus.Util.Bitmap` providing `ByteString`-based compact bitmaps.
- Define concrete `PerasVote` and `PerasCert` types using BLS signatures.
- Define `PerasCrypto` scheme with support for all the voting committee superclasses.
- Define `VotingCommitteeSupportsPeras` type class with conversions between concrete Peras types and their abstract counterparts.
- Instantiate `VotingCommitteeSupportsPeras` for both WFALS and EveryoneVotes.

<!--
### Patch

- A bullet item for the Patch category.

-->
