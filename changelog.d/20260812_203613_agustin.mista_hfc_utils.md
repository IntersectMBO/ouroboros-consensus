<!--
A new scriv changelog fragment.

Uncomment the section that is right (remove the HTML comment wrapper).
For top level release notes, leave all the headers commented out.
-->

<!--
### Breaking

- A bullet item for the Breaking category.

-->
### Non-Breaking

- Add ensureSameEraPair, ensureSameEraNonEmpty, alignNPWithNS, and hcollect
  utility helpers for Hard Fork Combinator era-indexed values.

- Add SOP helpers to implement HFC dispatching for Peras:
    - `ensureSameEra*` family of functions,
    - `alignNPWithNS`,
    - higher order Either-like type `EitherF` and `mkEitherF` constructor,
    - `hcollect` for `EitherF`.

<!--
### Patch

- A bullet item for the Patch category.

-->
