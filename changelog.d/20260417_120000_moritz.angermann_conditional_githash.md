### Patch

- Make `githash` dependency conditional via the `external-git-rev` Cabal flag,
  fixing `-Wunused-packages` errors in Nix builds where the git revision is
  provided at compile time through CPP.
