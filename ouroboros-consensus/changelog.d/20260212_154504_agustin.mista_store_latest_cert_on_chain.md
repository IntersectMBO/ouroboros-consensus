<!--
A new scriv changelog fragment.

Uncomment the section that is right (remove the HTML comment wrapper).
For top level release notes, leave all the headers commented out.
-->

<!--
### Patch

- A bullet item for the Patch category.

-->

### Non-Breaking

- Add `getPerasCertInBlock` method to `BlockSupportsPeras` typeclass.
- Add `LedgerSupportsPeras` typeclass.
- Add instance `LedgerSupportsPeras` for `HardForkBlock`.
- Extend block body and ledger state for `TestBlock` to support containing and extracting Peras certificate round numbers.
- Add `getLatestPerasCertOnChainRound` method to `ChainDB` API.
- Extend `ChainDB` QSM tests with `GetLatestPerasCertOnChainRound` command.


<!--
### Breaking

- A bullet item for the Breaking category.

-->
